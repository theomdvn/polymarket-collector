services:
  collector:
    build: .
    volumes:
      - .:/app
      - ./storage:/app/storage
      - ${BACKTEST_DATA_DIR:-../polymarket-backtest/data}:/backtest-data
    env_file:
      - .env
    command: python main.py
    restart: unless-stopped
